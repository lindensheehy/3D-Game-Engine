#pragma once

#include "util/Utility.h"

/*
    This file is responsible for reading from text files.
    This can read any file format, but it will interpret it always as text.
    Returns the data in a 2d char array, where each element in a full line in the file.
*/

